{
  "name": "youtube-feed-customizer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Chrome extension that filters the YouTube home feed with a natural-language rule, judged by Jev.",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "@vercel/node": "^13.0.2",
    "prettier": "^3.9.9",
    "tsx": "^4.23.15",
    "typescript": "^5.6.0",
    "wxt": "^0.21.0"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/samaksh-bajaj/youtube-feed-customizer.git"
  }
}
